using AutoMapper;
using System;

namespace WorkTimer.Common.TypeConverters
{
    /*public class TimeSpanToStringDataConverter : TypeConverter<string, TimeSpan?>
    {
        public TimeSpan? Convert(
            string source,
            TimeSpan? destination,
            ResolutionContext context)
        {
            if (source == null)
            {
                return null;
            }

            return TimeSpan.Parse(source);
        }
    }*/
}

